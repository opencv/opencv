set(__extra_modules "")
if(HAVE_opencv_dnn)
  list(APPEND __extra_modules "opencv_dnn")
endif()
ocv_add_application(opencv_version MODULES opencv_core ${__extra_modules} SRCS opencv_version.cpp)
if(WIN32)
  ocv_add_application(opencv_version_win32 MODULES opencv_core SRCS opencv_version.cpp)
  target_compile_definitions(opencv_version_win32 PRIVATE "OPENCV_WIN32_API=1")
endif()
