/*
 * file:   LineSegmentDetectorBridge.cpp
 * author: A trusty code generator
 * date:   Tue, 28 Feb 2017 10:45:57
 *
 * This file was autogenerated, do not modify.
 * See LICENSE for full modification and redistribution details.
 * Copyright 2017 The OpenCV Foundation
 */
#include <mex.h>
#include <vector>
#include <string>
#include <opencv2/matlab/map.hpp>
#include <opencv2/matlab/bridge.hpp>
#include <opencv2/core.hpp>
using namespace cv;
using namespace matlab;
using namespace bridge;

namespace {

typedef std::vector<Bridge> (*)(LineSegmentDetector&, const std::vector<Bridge>&) MethodSignature;


// wrapper for detect() method
std::vector<Bridge> detect(LineSegmentDetector& inst, const std::vector<Bridge>& inputs) {
  std::vector<Bridge> outputs(4);
  
  // unpack the arguments
  Mat _image = inputs[0].toMat();
  Mat _lines;
  Mat width;
  Mat prec;
  Mat nfa;

  // call the opencv function
  // [out =] namespace.fun(src1, ..., srcn, dst1, ..., dstn, opt1, ..., optn);
  try {
    inst.detect(_image, _lines, width, prec, nfa);
  } catch(cv::Exception& e) {
    error(std::string("cv::exception caught: ").append(e.what()).c_str());
  } catch(std::exception& e) {
    error(std::string("std::exception caught: ").append(e.what()).c_str());
  } catch(...) {
    error("Uncaught exception occurred in detect");
  }
  
  // assign the outputs into the bridge
  outputs[0] = _lines;
  outputs[1] = width;
  outputs[2] = prec;
  outputs[3] = nfa;

  return outputs;
}

// wrapper for drawSegments() method
std::vector<Bridge> drawSegments(LineSegmentDetector& inst, const std::vector<Bridge>& inputs) {
  std::vector<Bridge> outputs(1);
  
  // unpack the arguments
  Mat _image = inputs[0].toMat();
  Mat lines = inputs[1].toMat();

  // call the opencv function
  // [out =] namespace.fun(src1, ..., srcn, dst1, ..., dstn, opt1, ..., optn);
  try {
    inst.drawSegments(_image, lines);
  } catch(cv::Exception& e) {
    error(std::string("cv::exception caught: ").append(e.what()).c_str());
  } catch(std::exception& e) {
    error(std::string("std::exception caught: ").append(e.what()).c_str());
  } catch(...) {
    error("Uncaught exception occurred in drawSegments");
  }
  
  // assign the outputs into the bridge
  outputs[0] = _image;

  return outputs;
}

// wrapper for compareSegments() method
std::vector<Bridge> compareSegments(LineSegmentDetector& inst, const std::vector<Bridge>& inputs) {
  std::vector<Bridge> outputs(2);
  
  // unpack the arguments
  Size size = inputs[0].toSize();
  Mat lines1 = inputs[1].toMat();
  Mat lines2 = inputs[2].toMat();
  Mat _image = inputs[3].empty() ? (Mat) Mat() : inputs[3].toMat();
  int retval;

  // call the opencv function
  // [out =] namespace.fun(src1, ..., srcn, dst1, ..., dstn, opt1, ..., optn);
  try {
    retval = inst.compareSegments(size, lines1, lines2, _image);
  } catch(cv::Exception& e) {
    error(std::string("cv::exception caught: ").append(e.what()).c_str());
  } catch(std::exception& e) {
    error(std::string("std::exception caught: ").append(e.what()).c_str());
  } catch(...) {
    error("Uncaught exception occurred in compareSegments");
  }
  
  // assign the outputs into the bridge
  outputs[0] = retval;
  outputs[1] = _image;

  return outputs;
}

Map<std::string, MethodSignature> createMethodMap() {
  Map<std::string, MethodSignature> m;
  m["detect"] = &detect;
  m["drawSegments"] = &drawSegments;
  m["compareSegments"] = &compareSegments;

  return m;
}
static const Map<std::string, MethodSignature> methods = createMethodMap();

// map of created LineSegmentDetector instances. Don't trust the user to keep them safe...
static Map<void *, LineSegmentDetector> instances;

/*
 * LineSegmentDetector
 * Gateway routine
 *   nlhs - number of return arguments
 *   plhs - pointers to return arguments
 *   nrhs - number of input arguments
 *   prhs - pointers to input arguments
 */
void mexFunction(int nlhs, mxArray* plhs[],
                 int nrhs, const mxArray* prhs[]) {

  // parse the inputs
  Bridge method_name(prhs[0]);

  Bridge handle(prhs[1]);
  std::vector<Bridge> brhs(prhs+2, prhs+nrhs);

  // retrieve the instance of interest
  try {
    LineSegmentDetector& inst = instances.at(handle.address());
  } catch (const std::out_of_range& e) {
    mexErrMsgTxt("Invalid object instance provided");
  }

  // invoke the correct method on the data
  try {
    std::vector<Bridge> blhs = (*methods.at(method_name))(inst, brhs);
  } catch (const std::out_of_range& e) {
    mexErrMsgTxt("Unknown method specified");
  }



}

} // end namespace