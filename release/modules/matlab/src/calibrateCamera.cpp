/*
 * file:   calibrateCamera.cpp
 * author: A trusty code generator
 * date:   Tue, 28 Feb 2017 10:45:57
 *
 * This file was autogenerated, do not modify.
 * See LICENSE for full modification and redistribution details.
 * Copyright 2017 The OpenCV Foundation
 */
#include <string>
#include <vector>
#include <cassert>
#include <exception>
#include <opencv2/matlab/bridge.hpp>
#include <opencv2/calib3d.hpp>
using namespace cv;
using namespace matlab;
using namespace bridge;

/*
 * calibrateCamera
 * double calibrateCamera(vector_Mat objectPoints, vector_Mat imagePoints, Size imageSize, Mat cameraMatrix, Mat distCoeffs, vector_Mat rvecs, vector_Mat tvecs, int flags=0, TermCriteria criteria=TermCriteria( TermCriteria::COUNT + TermCriteria::EPS, 30, DBL_EPSILON));
 * Gateway routine
 *   nlhs - number of return arguments
 *   plhs - pointers to return arguments
 *   nrhs - number of input arguments
 *   prhs - pointers to input arguments
 */
void mexFunction(int nlhs, mxArray* plhs[],
                 int nrhs, const mxArray* prhs[]) {

  // parse the inputs
  ArgumentParser parser("calibrateCamera");
  parser.addVariant("calibrateCamera", 5, 2, "flags", "criteria");
  MxArrayVector sorted = parser.parse(MxArrayVector(prhs, prhs+nrhs));

  // setup
  BridgeVector inputs(sorted.begin(), sorted.end());
  BridgeVector outputs(5);

  
  // unpack the arguments
  vector_Mat objectPoints = inputs[0].toVectorMat();
  vector_Mat imagePoints = inputs[1].toVectorMat();
  Size imageSize = inputs[2].toSize();
  Mat cameraMatrix = inputs[3].toMat();
  Mat distCoeffs = inputs[4].toMat();
  int flags = inputs[5].empty() ? (int) 0 : inputs[5].toInt();
  TermCriteria criteria = inputs[6].empty() ? (TermCriteria) TermCriteria( TermCriteria::COUNT + TermCriteria::EPS, 30, DBL_EPSILON) : inputs[6].toTermCriteria();
  vector_Mat rvecs;
  vector_Mat tvecs;
  double retval;

  // call the opencv function
  // [out =] namespace.fun(src1, ..., srcn, dst1, ..., dstn, opt1, ..., optn);
  try {
    retval = cv::calibrateCamera(objectPoints, imagePoints, imageSize, cameraMatrix, distCoeffs, rvecs, tvecs, flags, criteria);
  } catch(cv::Exception& e) {
    error(std::string("cv::exception caught: ").append(e.what()).c_str());
  } catch(std::exception& e) {
    error(std::string("std::exception caught: ").append(e.what()).c_str());
  } catch(...) {
    error("Uncaught exception occurred in calibrateCamera");
  }
  
  // assign the outputs into the bridge
  outputs[0] = retval;
  outputs[1] = cameraMatrix;
  outputs[2] = distCoeffs;
  outputs[3] = rvecs;
  outputs[4] = tvecs;


  // push the outputs back to matlab
  for (size_t n = 0; n < static_cast<size_t>(std::max(nlhs,1)); ++n) {
    plhs[n] = outputs[n].toMxArray().releaseOwnership();
  }
}