/*
 * file:   colorChange.cpp
 * author: A trusty code generator
 * date:   Tue, 28 Feb 2017 10:45:57
 *
 * This file was autogenerated, do not modify.
 * See LICENSE for full modification and redistribution details.
 * Copyright 2017 The OpenCV Foundation
 */
#include <string>
#include <vector>
#include <cassert>
#include <exception>
#include <opencv2/matlab/bridge.hpp>
#include <opencv2/photo.hpp>
using namespace cv;
using namespace matlab;
using namespace bridge;

/*
 * colorChange
 * void colorChange(Mat src, Mat mask, Mat dst, float red_mul=1.0f, float green_mul=1.0f, float blue_mul=1.0f);
 * Gateway routine
 *   nlhs - number of return arguments
 *   plhs - pointers to return arguments
 *   nrhs - number of input arguments
 *   prhs - pointers to input arguments
 */
void mexFunction(int nlhs, mxArray* plhs[],
                 int nrhs, const mxArray* prhs[]) {

  // parse the inputs
  ArgumentParser parser("colorChange");
  parser.addVariant("colorChange", 2, 3, "red_mul", "green_mul", "blue_mul");
  MxArrayVector sorted = parser.parse(MxArrayVector(prhs, prhs+nrhs));

  // setup
  BridgeVector inputs(sorted.begin(), sorted.end());
  BridgeVector outputs(1);

  
  // unpack the arguments
  Mat src = inputs[0].toMat();
  Mat mask = inputs[1].toMat();
  float red_mul = inputs[2].empty() ? (float) 1.0f : inputs[2].toFloat();
  float green_mul = inputs[3].empty() ? (float) 1.0f : inputs[3].toFloat();
  float blue_mul = inputs[4].empty() ? (float) 1.0f : inputs[4].toFloat();
  Mat dst;

  // call the opencv function
  // [out =] namespace.fun(src1, ..., srcn, dst1, ..., dstn, opt1, ..., optn);
  try {
    cv::colorChange(src, mask, dst, red_mul, green_mul, blue_mul);
  } catch(cv::Exception& e) {
    error(std::string("cv::exception caught: ").append(e.what()).c_str());
  } catch(std::exception& e) {
    error(std::string("std::exception caught: ").append(e.what()).c_str());
  } catch(...) {
    error("Uncaught exception occurred in colorChange");
  }
  
  // assign the outputs into the bridge
  outputs[0] = dst;


  // push the outputs back to matlab
  for (size_t n = 0; n < static_cast<size_t>(std::max(nlhs,1)); ++n) {
    plhs[n] = outputs[n].toMxArray().releaseOwnership();
  }
}