/*
 * file:   buildOpticalFlowPyramid.cpp
 * author: A trusty code generator
 * date:   Tue, 28 Feb 2017 10:45:57
 *
 * This file was autogenerated, do not modify.
 * See LICENSE for full modification and redistribution details.
 * Copyright 2017 The OpenCV Foundation
 */
#include <string>
#include <vector>
#include <cassert>
#include <exception>
#include <opencv2/matlab/bridge.hpp>
#include <opencv2/video.hpp>
using namespace cv;
using namespace matlab;
using namespace bridge;

/*
 * buildOpticalFlowPyramid
 * int buildOpticalFlowPyramid(Mat img, vector_Mat pyramid, Size winSize, int maxLevel, bool withDerivatives=true, int pyrBorder=BORDER_REFLECT_101, int derivBorder=BORDER_CONSTANT, bool tryReuseInputImage=true);
 * Gateway routine
 *   nlhs - number of return arguments
 *   plhs - pointers to return arguments
 *   nrhs - number of input arguments
 *   prhs - pointers to input arguments
 */
void mexFunction(int nlhs, mxArray* plhs[],
                 int nrhs, const mxArray* prhs[]) {

  // parse the inputs
  ArgumentParser parser("buildOpticalFlowPyramid");
  parser.addVariant("buildOpticalFlowPyramid", 3, 4, "withDerivatives", "pyrBorder", "derivBorder", "tryReuseInputImage");
  MxArrayVector sorted = parser.parse(MxArrayVector(prhs, prhs+nrhs));

  // setup
  BridgeVector inputs(sorted.begin(), sorted.end());
  BridgeVector outputs(2);

  
  // unpack the arguments
  Mat img = inputs[0].toMat();
  Size winSize = inputs[1].toSize();
  int maxLevel = inputs[2].toInt();
  bool withDerivatives = inputs[3].empty() ? (bool) true : inputs[3].toBool();
  int pyrBorder = inputs[4].empty() ? (int) BORDER_REFLECT_101 : inputs[4].toInt();
  int derivBorder = inputs[5].empty() ? (int) BORDER_CONSTANT : inputs[5].toInt();
  bool tryReuseInputImage = inputs[6].empty() ? (bool) true : inputs[6].toBool();
  vector_Mat pyramid;
  int retval;

  // call the opencv function
  // [out =] namespace.fun(src1, ..., srcn, dst1, ..., dstn, opt1, ..., optn);
  try {
    retval = cv::buildOpticalFlowPyramid(img, pyramid, winSize, maxLevel, withDerivatives, pyrBorder, derivBorder, tryReuseInputImage);
  } catch(cv::Exception& e) {
    error(std::string("cv::exception caught: ").append(e.what()).c_str());
  } catch(std::exception& e) {
    error(std::string("std::exception caught: ").append(e.what()).c_str());
  } catch(...) {
    error("Uncaught exception occurred in buildOpticalFlowPyramid");
  }
  
  // assign the outputs into the bridge
  outputs[0] = retval;
  outputs[1] = pyramid;


  // push the outputs back to matlab
  for (size_t n = 0; n < static_cast<size_t>(std::max(nlhs,1)); ++n) {
    plhs[n] = outputs[n].toMxArray().releaseOwnership();
  }
}