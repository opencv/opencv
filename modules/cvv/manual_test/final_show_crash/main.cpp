#include "final_show.hpp"

/**
 * @brief This test just calls finalShow.
 * 
 * The expected behaviour is that basically nothing will happen before the
 * program terminates without crash.
 */
int main()
{
	cvv::finalShow();
}
