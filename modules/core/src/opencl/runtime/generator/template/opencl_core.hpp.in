#ifndef OPENCV_CORE_OCL_RUNTIME_OPENCL_CORE_HPP
#error "Invalid usage"
#endif

@CL_REMAP_ORIGIN@

#if defined __APPLE__
#define CL_SILENCE_DEPRECATION
#include <OpenCL/cl.h>
#else
#include <CL/cl.h>
#endif

@CL_REMAP_DYNAMIC@

@CL_FN_DECLARATIONS@
