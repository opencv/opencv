@startuml
package "TrackerSampler package" #DDDDDD {

enum TrackerSamplerType{
	TRACKER_SAMPLER_CSC
	TRACKER_SAMPLER_CS
}

class TrackerSampler{
	-vector<pair<String, Ptr<TrackerSamplerAlgorithm> > > samplers
	-vector<Mat> samples;
	...
	TrackerSampler();
	~TrackerSampler();
	+sampling(const Mat& image, Point2f position);
	+vector<pair<String, Ptr<TrackerSamplerAlgorithm> > > getSamples();
	+bool addTrackerSamplerAlgorithm(String trackerSamplerAlgorithmType);
	---
	-void clearSamples();
}

class TrackerSamplerAlgorithm{
	~TrackerSamplerAlgorithm();
	+Ptr<TrackerSamplerAlgorithm> create(String trackerSamplerType);
	+bool sampling(const Mat& image, Point2f position, vector<Mat>& sample);
}
note bottom: A tracker could sample the target\nor it could sample the target and the background


class TrackerSamplerCS{
	TrackerSamplerCS();
	~TrackerSamplerCS();
	+bool sampling(const Mat& image, Point2f position, vector<Mat>& sample);
}
class TrackerSamplerCSC{
	TrackerSamplerCSC();
	~TrackerSamplerCSC();
	+bool sampling(const Mat& image, Point2f position, vector<Mat>& sample);
}



TrackerSampler *-- TrackerSamplerAlgorithm
TrackerSamplerAlgorithm <|-- TrackerSamplerCS
TrackerSamplerAlgorithm <|-- TrackerSamplerCSC
}
@enduml