@startuml
package "Tracker package" #DDDDDD {

enum TrackerType {
  	TRACKER_MIL
  	TRACKER_BOOSTING
  	. . .
}
enum TrackerType
note right: Algorithms


class Algorithm {
}
class Tracker{
	+static Ptr<Tracker> create(string trackerType, TrackerParams p);
	+bool init(const Mat& image, const Rect& boundingBox);
	+bool update(const Mat& image, Rect& boundingBox);
}
class Tracker 
note right: Tracker is the general interface for each specialized trackers
class TrackerMIL{
	+Params
	---
	TrackerMIL(TrackerMIL::Params param);
	+bool init(const Mat& image, const Rect& boundingBox);
	+bool update(const Mat& image, Rect& boundingBox);
}
class TrackerBoosting{
	+Params
	---
	TrackerBoosting(TrackerBoosting::Params param);
	+bool init(const Mat& image, const Rect& boundingBox);
	+bool update(const Mat& image, Rect& boundingBox);
}
Algorithm <|-- Tracker : virtual inheritance 
Tracker <|-- TrackerMIL
Tracker <|-- TrackerBoosting

note "Single instance of the Tracker" as N1
TrackerBoosting .. N1
TrackerMIL .. N1
}

@enduml