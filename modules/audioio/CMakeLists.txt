ocv_add_module(audioio opencv_imgproc opencv_imgcodecs WRAP java python)
if(WITH_ALSA)
  include(cmake/FindALSA.cmake)
  set(HAVE_ALSA TRUE)
endif(WITH_ALSA)

if(WITH_SNDFILE)
  include(cmake/FindLIBSNDFILE.cmake)
  set(HAVE_SNDFILE TRUE)
endif(WITH_SNDFILE)
